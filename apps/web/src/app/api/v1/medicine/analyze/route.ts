import {NextResponse} from "next/server";import {medicineAnalyze,updateMedicineExtraction} from "@/lib/operations";import {extractMedicinePackage} from "@/lib/vision";
export async function POST(req:Request){try{const b=await req.json();if(!b.fileName)return NextResponse.json({error:"fileName required"},{status:400});const base=await medicineAnalyze(b.patientId??"patient-amal",b.fileName);let providerUsed=false;let providerError:string|undefined;if(b.imageDataUrl&&process.env.OPENAI_API_KEY){try{const extracted=await extractMedicinePackage(b.imageDataUrl);base.session=await updateMedicineExtraction(base.session.id,extracted);providerUsed=true;}catch(e:any){providerError=e.message;}}const expiry=base.session.extracted.expiryDateISO;base.expired=expiry?new Date(`${expiry}T23:59:59Z`)<new Date():false;return NextResponse.json({...base,providerUsed,providerError})}catch(e:any){return NextResponse.json({error:e.message},{status:400})}}
