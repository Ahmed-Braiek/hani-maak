import {NextResponse} from "next/server";import {completeVisit} from "@/lib/operations";import {requirePermission} from "@/lib/staff-auth";import {recordStaffAudit} from "@/lib/audit";
export async function POST(_:Request,{params}:{params:Promise<{id:string}>}){try{const identity=await requirePermission("appointments.edit");const {id}=await params;const appointment=await completeVisit(id);await recordStaffAudit(identity,"appointment.complete","appointment",id,{state:appointment.state});return NextResponse.json({appointment})}catch(e:any){const status=e?.status===403?403:400;return NextResponse.json({error:status===403?"forbidden":"appointment_update_failed"},{status})}}
