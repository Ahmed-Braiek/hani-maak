import {NextResponse} from "next/server";import {mutateDb} from "@/lib/db";
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){try{const {id}=await params;const b=await req.json();const node=await mutateDb(db=>{const x=db.facilityNodes.find(v=>v.id===id);if(!x)throw new Error("Node not found");if(typeof b.labelFr==="string")x.label.fr=b.labelFr.slice(0,100);if(Number.isFinite(b.x)&&b.x>=0&&b.x<=100)x.x=b.x;if(Number.isFinite(b.y)&&b.y>=0&&b.y<=100)x.y=b.y;x.provenance="demo_seeded";return x});return NextResponse.json({node})}catch(e:any){return NextResponse.json({error:e.message},{status:400})}}
