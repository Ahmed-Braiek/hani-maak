import {NextResponse} from "next/server";import {mutateDb} from "@/lib/db";
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){try{const {id}=await params;const b=await req.json();const content=await mutateDb(db=>{const x=db.contentTemplates.find(v=>v.id===id);if(!x)throw new Error("Content not found");if(typeof b.bodyFr==="string")x.body.fr=b.bodyFr.slice(0,2000);if(typeof b.published==="boolean")x.published=b.published;x.version++;return x});return NextResponse.json({content})}catch(e:any){return NextResponse.json({error:e.message},{status:400})}}
