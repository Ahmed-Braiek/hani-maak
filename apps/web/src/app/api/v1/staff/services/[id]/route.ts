import {NextResponse} from "next/server";import {mutateDb} from "@/lib/db";
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){try{const {id}=await params;const b=await req.json();const s=await mutateDb(db=>{const x=db.services.find(v=>v.id===id);if(!x)throw new Error("Service not found");if(typeof b.nameFr==="string")x.name.fr=b.nameFr.slice(0,100);if(Number.isFinite(b.slotDurationMin)&&b.slotDurationMin>=10&&b.slotDurationMin<=120){x.slotDurationMin=b.slotDurationMin;for(const r of db.scheduleRules.filter(r=>r.serviceId===id))r.slotDurationMin=b.slotDurationMin;}if(typeof b.active==="boolean")x.active=b.active;return x});return NextResponse.json({service:s})}catch(e:any){return NextResponse.json({error:e.message},{status:400})}}
