import {NextResponse} from "next/server";import {readDb} from "@/lib/db";import {getAvailableSlots} from "@/lib/scheduling";
export async function POST(req:Request){try{const body=await req.json();if(!body.serviceId||!body.fromDate||!body.toDate)return NextResponse.json({error:"serviceId, fromDate and toDate are required"},{status:400});const db=await readDb();const slots=getAvailableSlots(db,body.serviceId,body.fromDate,body.toDate,body.preferredPeriod,12);return NextResponse.json({slots,policyVersion:"demo-schedule-v1"})}catch(e:any){return NextResponse.json({error:e.message},{status:400})}}
