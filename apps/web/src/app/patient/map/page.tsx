import {PatientShell} from "@/components/PatientShell";import {MapClient} from "@/components/MapClient";import {patientSnapshot} from "@/lib/operations";import {getServerLocale} from "@/lib/i18n-server";import {t} from "@/lib/i18n";
export const dynamic="force-dynamic";export default async function MapPage(){const [x,locale]=await Promise.all([patientSnapshot(),getServerLocale()]);const serviceId=x.activeAppointment?.serviceId??"svc-imaging";return <PatientShell locale={locale}><div className="eyebrow">{t(locale,"map.eyebrow")}</div><h1 className="mobile-title">{t(locale,"map.title")}</h1><p className="muted">{t(locale,"map.lead")}</p><MapClient serviceId={serviceId}/></PatientShell>}
