import {StaffShell} from "@/components/StaffShell";import {readDb} from "@/lib/db";import {BrandSettingsClient} from "@/components/StaffEditors";import {DemoResetClient} from "@/components/DemoResetClient";
export const dynamic="force-dynamic";
export default async function Settings(){const db=await readDb();return <StaffShell><div className="page-head"><div><div className="eyebrow">White-label tenant</div><h1>Settings</h1><div className="muted">Branding and demo controls are tenant configuration, not frontend hard-coding.</div></div></div><div className="two-col"><div className="card"><h3>Provider identity</h3><BrandSettingsClient brand={db.tenant.brand} displayName={db.tenant.displayName}/></div><div className="stack"><div className="card"><h3>Competition mode</h3><p className="muted tiny">Synthetic identity shortcut, seeded indoor progress controls and provenance labels are enabled.</p><div className="row"><span>DEMO_MODE</span><span className="badge good">true</span></div></div><div className="card"><h3>Reset known state</h3><p className="muted tiny">Use immediately before presenting. This removes all actions created during practice and reloads the synthetic seed.</p><DemoResetClient/></div></div></div></StaffShell>}
