import {StaffShell} from "@/components/StaffShell";import {staffSnapshot} from "@/lib/operations";import {Badge} from "@/components/Badge";import {AppointmentActionClient} from "@/components/AppointmentActionClient";
export const dynamic="force-dynamic";
export default async function Appointments(){const x=await staffSnapshot();return <StaffShell><div className="page-head"><div><div className="eyebrow">Scheduling</div><h1>Appointments</h1><div className="muted">Synthetic schedule state shared with patient and voice channels.</div></div></div><div className="table-wrap"><table className="table"><thead><tr><th>Patient</th><th>Service</th><th>Time</th><th>Channel</th><th>Status</th><th>Action</th></tr></thead><tbody>{x.appointments.sort((a,b)=>a.startAt.localeCompare(b.startAt)).map(a=>{const p=x.patients.find(v=>v.id===a.patientId);const s=x.services.find(v=>v.id===a.serviceId);return <tr key={a.id}><td><strong>{p?.firstName} {p?.lastName}</strong><div className="muted tiny">{a.id}</div></td><td>{s?.name.fr}</td><td>{new Intl.DateTimeFormat("fr-TN",{dateStyle:"medium",timeStyle:"short",timeZone:"Africa/Tunis"}).format(new Date(a.startAt))}</td><td><Badge tone="info">{a.channel}</Badge></td><td><Badge tone={a.state==="confirmed"?"good":a.state==="cancelled"?"danger":a.state==="completed"?"info":""}>{a.state}</Badge></td><td><AppointmentActionClient id={a.id} state={a.state}/></td></tr>})}</tbody></table></div></StaffShell>}
