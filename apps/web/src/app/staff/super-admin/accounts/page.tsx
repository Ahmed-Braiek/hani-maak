import {StaffShell} from "@/components/StaffShell";import {AccountManagementClient} from "@/components/AccountManagementClient";import {readDb} from "@/lib/db";import {getPlatformAccounts} from "@/lib/staff-store";export const dynamic="force-dynamic";export default async function Accounts(){const db=await readDb();return <StaffShell role="super_admin"><AccountManagementClient initial={getPlatformAccounts(db)}/></StaffShell>}
