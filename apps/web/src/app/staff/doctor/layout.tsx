import {redirect} from "next/navigation";import {requireRole} from "@/lib/staff-auth";export default async function Layout({children}:{children:React.ReactNode}){try{await requireRole("doctor")}catch{redirect("/staff/denied")}return children}
