"use client";
import {useState} from "react";import {useRouter} from "next/navigation";
export function BrandSettingsClient({brand,displayName}:{brand:{primary:string;accent:string;logoText:string};displayName:string}){const router=useRouter();const [name,setName]=useState(displayName);const [primary,setPrimary]=useState(brand.primary);const [accent,setAccent]=useState(brand.accent);const [busy,setBusy]=useState(false);async function save(){setBusy(true);await fetch("/api/v1/staff/settings",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({displayName:name,brand:{...brand,primary,accent}})});setBusy(false);router.refresh()}return <div className="form"><div className="field"><label>Provider display name</label><input className="input" value={name} onChange={e=>setName(e.target.value)}/></div><div className="row"><div className="field" style={{flex:1}}><label>Primary</label><input className="input" type="color" value={primary} onChange={e=>setPrimary(e.target.value)}/></div><div className="field" style={{flex:1}}><label>Accent</label><input className="input" type="color" value={accent} onChange={e=>setAccent(e.target.value)}/></div></div><button className="btn btn-primary" onClick={save} disabled={busy}>{busy?"Saving…":"Save tenant branding"}</button></div>}
export function ServiceEditorClient({service}:{service:any}){const router=useRouter();const [name,setName]=useState(service.name.fr);const [duration,setDuration]=useState(service.slotDurationMin);const [active,setActive]=useState(service.active);const [busy,setBusy]=useState(false);async function save(){setBusy(true);await fetch(`/api/v1/staff/services/${service.id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({nameFr:name,slotDurationMin:Number(duration),active})});setBusy(false);router.refresh()}return <div className="form"><div className="field"><label>French name</label><input className="input" value={name} onChange={e=>setName(e.target.value)}/></div><div className="row"><div className="field" style={{flex:1}}><label>Slot duration (min)</label><input className="input" type="number" min={10} max={120} value={duration} onChange={e=>setDuration(Number(e.target.value))}/></div><label className="row" style={{alignSelf:"end",minHeight:46}}><input type="checkbox" checked={active} onChange={e=>setActive(e.target.checked)}/> Active</label></div><button className="btn btn-secondary" onClick={save} disabled={busy}>{busy?"Saving…":"Save service"}</button></div>}
export function ContentEditorClient({content}:{content:any}){const router=useRouter();const [body,setBody]=useState(content.body.fr);const [published,setPublished]=useState(content.published);const [busy,setBusy]=useState(false);async function save(){setBusy(true);await fetch(`/api/v1/staff/content/${content.id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({bodyFr:body,published})});setBusy(false);router.refresh()}return <div className="form"><textarea className="textarea" rows={4} value={body} onChange={e=>setBody(e.target.value)}/><div className="row"><label><input type="checkbox" checked={published} onChange={e=>setPublished(e.target.checked)}/> Published</label><button className="btn btn-secondary" onClick={save} disabled={busy}>Save</button></div></div>}
export function NodeEditorClient({node}:{node:any}){const router=useRouter();const [label,setLabel]=useState(node.label.fr);const [x,setX]=useState(node.x);const [y,setY]=useState(node.y);const [busy,setBusy]=useState(false);async function save(){setBusy(true);await fetch(`/api/v1/staff/map/nodes/${node.id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({labelFr:label,x:Number(x),y:Number(y)})});setBusy(false);router.refresh()}return <div className="row" style={{alignItems:"end"}}><div className="field" style={{flex:2}}><label>Label</label><input className="input" value={label} onChange={e=>setLabel(e.target.value)}/></div><div className="field" style={{width:90}}><label>X</label><input className="input" type="number" min={0} max={100} value={x} onChange={e=>setX(Number(e.target.value))}/></div><div className="field" style={{width:90}}><label>Y</label><input className="input" type="number" min={0} max={100} value={y} onChange={e=>setY(Number(e.target.value))}/></div><button className="btn btn-secondary" onClick={save} disabled={busy}>Save</button></div>}
export function ScheduleRuleEditor({rule}:{rule:any}){const router=useRouter();const [start,setStart]=useState(rule.startTime);const [end,setEnd]=useState(rule.endTime);const [capacity,setCapacity]=useState(rule.capacity);const [busy,setBusy]=useState(false);async function save(){setBusy(true);await fetch(`/api/v1/staff/schedules/${rule.id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({startTime:start,endTime:end,capacity:Number(capacity)})});setBusy(false);router.refresh()}return <div className="row" style={{alignItems:"end",flexWrap:"wrap"}}><span className="badge info">Day {rule.dayOfWeek}</span><div className="field"><label>Start</label><input className="input" type="time" value={start} onChange={e=>setStart(e.target.value)}/></div><div className="field"><label>End</label><input className="input" type="time" value={end} onChange={e=>setEnd(e.target.value)}/></div><div className="field" style={{width:90}}><label>Capacity</label><input className="input" type="number" min={1} max={50} value={capacity} onChange={e=>setCapacity(Number(e.target.value))}/></div><button className="btn btn-secondary" onClick={save} disabled={busy}>Save rule</button></div>}
