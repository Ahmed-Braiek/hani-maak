"use client";
import {useState} from "react";import {useRouter} from "next/navigation";import {usePersistentLocale} from "@/lib/locale-client";import {t} from "@/lib/i18n";import {stx} from "@/lib/staff-i18n";
export function BrandSettingsClient({brand,displayName}:{brand:{primary:string;accent:string;logoText:string};displayName:string}){const router=useRouter();const {locale}=usePersistentLocale();const [name,setName]=useState(displayName);const [primary,setPrimary]=useState(brand.primary);const [accent,setAccent]=useState(brand.accent);const [busy,setBusy]=useState(false);async function save(){setBusy(true);await fetch("/api/v1/staff/settings",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({displayName:name,brand:{...brand,primary,accent}})});setBusy(false);router.refresh()}return <div className="form"><div className="field"><label>{stx(locale,"editor.provider_name")}</label><input className="input" value={name} onChange={e=>setName(e.target.value)}/></div><div className="row"><div className="field" style={{flex:1}}><label>{stx(locale,"editor.primary")}</label><input className="input" type="color" value={primary} onChange={e=>setPrimary(e.target.value)}/></div><div className="field" style={{flex:1}}><label>{stx(locale,"editor.accent")}</label><input className="input" type="color" value={accent} onChange={e=>setAccent(e.target.value)}/></div></div><button className="btn btn-primary" onClick={save} disabled={busy}>{busy?stx(locale,"editor.saving"):stx(locale,"editor.save_brand")}</button></div>}
export function ServiceEditorClient({service}:{service:any}){const router=useRouter();const {locale}=usePersistentLocale();const [nameFr,setNameFr]=useState(service.name.fr);const [nameAr,setNameAr]=useState(service.name.ar);const [nameEn,setNameEn]=useState(service.name.en);const [duration,setDuration]=useState(service.slotDurationMin);const [active,setActive]=useState(service.active);const [busy,setBusy]=useState(false);async function save(){setBusy(true);await fetch(`/api/v1/staff/services/${service.id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({nameFr,nameAr,nameEn,slotDurationMin:Number(duration),active})});setBusy(false);router.refresh()}return <div className="form"><div className="staff-form-grid"><div className="field"><label>{stx(locale,"editor.name_fr")}</label><input className="input" value={nameFr} onChange={e=>setNameFr(e.target.value)}/></div><div className="field"><label>{stx(locale,"editor.name_ar")}</label><input className="input" dir="rtl" value={nameAr} onChange={e=>setNameAr(e.target.value)}/></div><div className="field"><label>{stx(locale,"editor.name_en")}</label><input className="input" value={nameEn} onChange={e=>setNameEn(e.target.value)}/></div><div className="field"><label>{stx(locale,"editor.slot_duration")}</label><input className="input" type="number" min={10} max={120} value={duration} onChange={e=>setDuration(Number(e.target.value))}/></div></div><label className="row"><input type="checkbox" checked={active} onChange={e=>setActive(e.target.checked)}/> {t(locale,"common.active")}</label><button className="btn btn-secondary" onClick={save} disabled={busy}>{busy?stx(locale,"editor.saving"):stx(locale,"editor.save_service")}</button></div>}
export function ContentEditorClient({content}:{content:any}){const router=useRouter();const {locale}=usePersistentLocale();const [fr,setFr]=useState(content.body.fr);const [ar,setAr]=useState(content.body.ar);const [en,setEn]=useState(content.body.en);const [published,setPublished]=useState(content.published);const [busy,setBusy]=useState(false);async function save(){setBusy(true);await fetch(`/api/v1/staff/content/${content.id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({bodyFr:fr,bodyAr:ar,bodyEn:en,published})});setBusy(false);router.refresh()}return <div className="form"><label>{stx(locale,"editor.name_fr")}<textarea className="textarea" rows={3} value={fr} onChange={e=>setFr(e.target.value)}/></label><label>{stx(locale,"editor.name_ar")}<textarea className="textarea" dir="rtl" rows={3} value={ar} onChange={e=>setAr(e.target.value)}/></label><label>{stx(locale,"editor.name_en")}<textarea className="textarea" rows={3} value={en} onChange={e=>setEn(e.target.value)}/></label><div className="row"><label><input type="checkbox" checked={published} onChange={e=>setPublished(e.target.checked)}/> {stx(locale,"editor.published")}</label><button className="btn btn-secondary" onClick={save} disabled={busy}>{busy?stx(locale,"editor.saving"):stx(locale,"editor.save_content")}</button></div></div>}
export function NodeEditorClient({node}:{node:any}){const router=useRouter();const {locale}=usePersistentLocale();const [fr,setFr]=useState(node.label.fr);const [ar,setAr]=useState(node.label.ar);const [en,setEn]=useState(node.label.en);const [x,setX]=useState(node.x);const [y,setY]=useState(node.y);const [busy,setBusy]=useState(false);async function save(){setBusy(true);await fetch(`/api/v1/staff/map/nodes/${node.id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({labelFr:fr,labelAr:ar,labelEn:en,x:Number(x),y:Number(y)})});setBusy(false);router.refresh()}return <div className="form"><div className="staff-form-grid"><label>{stx(locale,"editor.label_fr")}<input className="input" value={fr} onChange={e=>setFr(e.target.value)}/></label><label>{stx(locale,"editor.label_ar")}<input className="input" dir="rtl" value={ar} onChange={e=>setAr(e.target.value)}/></label><label>{stx(locale,"editor.label_en")}<input className="input" value={en} onChange={e=>setEn(e.target.value)}/></label><div className="row"><label>X<input className="input" type="number" min={0} max={100} value={x} onChange={e=>setX(Number(e.target.value))}/></label><label>Y<input className="input" type="number" min={0} max={100} value={y} onChange={e=>setY(Number(e.target.value))}/></label></div></div><button className="btn btn-secondary" onClick={save} disabled={busy}>{busy?stx(locale,"editor.saving"):t(locale,"common.save")}</button></div>}
export function ScheduleRuleEditor({rule}:{rule:any}){const router=useRouter();const {locale}=usePersistentLocale();const [start,setStart]=useState(rule.startTime);const [end,setEnd]=useState(rule.endTime);const [capacity,setCapacity]=useState(rule.capacity);const [busy,setBusy]=useState(false);async function save(){setBusy(true);await fetch(`/api/v1/staff/schedules/${rule.id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({startTime:start,endTime:end,capacity:Number(capacity)})});setBusy(false);router.refresh()}return <div className="row" style={{alignItems:"end",flexWrap:"wrap"}}><span className="badge info">{stx(locale,"editor.day")} · {stx(locale,`days.${rule.dayOfWeek}`)}</span><div className="field"><label>{stx(locale,"editor.start")}</label><input className="input" type="time" value={start} onChange={e=>setStart(e.target.value)}/></div><div className="field"><label>{stx(locale,"editor.end")}</label><input className="input" type="time" value={end} onChange={e=>setEnd(e.target.value)}/></div><div className="field" style={{width:100}}><label>{stx(locale,"editor.capacity")}</label><input className="input" type="number" min={1} max={50} value={capacity} onChange={e=>setCapacity(Number(e.target.value))}/></div><button className="btn btn-secondary" onClick={save} disabled={busy}>{busy?stx(locale,"editor.saving"):stx(locale,"editor.save_rule")}</button></div>}
