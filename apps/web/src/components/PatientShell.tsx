"use client";
import Link from "next/link";import {Logo} from "./Logo";import type {Locale} from "@/lib/types";import {text} from "@/lib/i18n";import {usePersistentLocale} from "@/lib/locale-client";
export function PatientShell({children,locale:initialLocale="fr"}:{children:React.ReactNode;locale?:Locale}){const {locale,setLocale,rtl}=usePersistentLocale(initialLocale);const x=text[locale];return <div className={`patient-shell ${rtl?"rtl":""}`} dir={rtl?"rtl":"ltr"}><header className="patient-head"><Logo compact/><div className="patient-head-actions"><span className="badge info">DEMO</span><div className="locale-switcher" aria-label="Language"><button className={locale==="fr"?"active":""} onClick={()=>setLocale("fr")} aria-pressed={locale==="fr"}>FR</button><button className={locale==="ar"?"active":""} onClick={()=>setLocale("ar")} aria-pressed={locale==="ar"}>ع</button><button className={locale==="en"?"active":""} onClick={()=>setLocale("en")} aria-pressed={locale==="en"}>EN</button></div></div></header><main className="patient-main">{children}</main><nav className="mobile-nav" aria-label="Patient navigation"><Link className="mobile-link" href="/patient"><span>⌂</span><span>{x.home}</span></Link><Link className="mobile-link" href="/patient/journey"><span>✓</span><span>{x.journey}</span></Link><Link className="mobile-link" href="/patient/map"><span>⌖</span><span>{x.map}</span></Link><Link className="mobile-link" href="/patient/profile"><span>○</span><span>{x.profile}</span></Link></nav></div>}
