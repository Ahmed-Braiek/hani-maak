"use client";
import Link from "next/link";import type {Service} from "@/lib/types";import {usePersistentLocale} from "@/lib/locale-client";import {t} from "@/lib/i18n";
export function PatientServicesClient({services}:{services:Service[]}){const {locale}=usePersistentLocale();return <><div className="eyebrow">{t(locale,"services.eyebrow")}</div><h1 className="mobile-title">{t(locale,"services.title")}</h1><p className="muted">{t(locale,"services.hint")}</p><div className="stack" style={{marginTop:20}}>{services.filter(s=>s.active).map(s=><Link className="service-card" key={s.id} href={`/patient/book/${s.id}`}><div className="row"><div className="service-card-copy"><div className="eyebrow">{s.department}</div><h3>{s.name[locale]}</h3><p className="muted tiny">{s.description[locale]}</p></div><span className="service-arrow" aria-hidden="true">{locale==="ar"?"←":"→"}</span></div></Link>)}</div></>}
