"use client";
import Link from "next/link";import type {Service} from "@/lib/types";import {usePersistentLocale} from "@/lib/locale-client";
const copy={fr:{eyebrow:"Accès",title:"De quel service avez-vous besoin ?",hint:"Choisissez un service pour passer directement au choix du rendez-vous."},ar:{eyebrow:"الوصول",title:"شنوة المصلحة اللي تحتاجها؟",hint:"اختار المصلحة وامشي مباشرة لاختيار الموعد."},en:{eyebrow:"Access",title:"Which service do you need?",hint:"Choose a service to go directly to appointment selection."}};
export function PatientServicesClient({services}:{services:Service[]}){const {locale}=usePersistentLocale();const c=copy[locale];return <><div className="eyebrow">{c.eyebrow}</div><h1 className="mobile-title">{c.title}</h1><p className="muted">{c.hint}</p><div className="stack" style={{marginTop:20}}>{services.filter(s=>s.active).map(s=><Link className="service-card" key={s.id} href={`/patient/book/${s.id}`}><div className="row"><div className="service-card-copy"><div className="eyebrow">{s.department}</div><h3>{s.name[locale]}</h3><p className="muted tiny">{s.description[locale]}</p></div><span className="service-arrow" aria-hidden="true">{locale==="ar"?"←":"→"}</span></div></Link>)}</div></>}
