"use client";import {useState} from "react";import {useRouter} from "next/navigation";import type {Journey} from "@/lib/types";import {usePersistentLocale} from "@/lib/locale-client";import {t,statusText} from "@/lib/i18n";
export function JourneyClient({journey}:{journey:Journey}){const [busy,setBusy]=useState<string>();const router=useRouter();const {locale}=usePersistentLocale();async function ack(id:string){setBusy(id);await fetch(`/api/v1/journey-steps/${id}/acknowledge`,{method:"POST"});setBusy(undefined);router.refresh()}return <div className="journey-list">{journey.steps.map(step=>{const acknowledgeable=step.payload?.acknowledgeable===true;return <div className="journey-step" key={step.id}><div className={`step-dot ${step.state}`}>{step.state==="completed"?"✓":step.sequence}</div><div><div className="row"><strong>{step.title[locale]??step.title.fr}</strong><span className={`badge ${step.state==="completed"?"good":step.state==="active"?"warn":""}`}>{statusText(locale,step.state)}</span></div>{step.body?.[locale]&&<p className="muted" style={{lineHeight:1.55,margin:"6px 0 10px"}}>{step.body[locale]}</p>}{step.state==="active"&&acknowledgeable&&<button className="btn btn-secondary" onClick={()=>ack(step.id)} disabled={busy===step.id}>{busy===step.id?t(locale,"journey.updating"):t(locale,"journey.understood")}</button>}{step.type==="navigation"&&step.state!=="completed"&&<a className="btn btn-secondary" href="/patient/map">{t(locale,"journey.open_guidance")}</a>}</div></div>})}</div>}
