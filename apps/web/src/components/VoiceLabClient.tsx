"use client";

import {useCallback,useEffect,useRef,useState} from "react";
import {HeniAvatar} from "./HeniAvatar";
import {usePersistentLocale} from "@/lib/locale-client";

type Msg={role:"bot"|"user";text:string;tool?:string;at?:string};
type RecognitionConstructor=new()=>any;
function recognitionConstructor():RecognitionConstructor|null{if(typeof window==="undefined")return null;const w=window as typeof window&{SpeechRecognition?:RecognitionConstructor;webkitSpeechRecognition?:RecognitionConstructor};return w.SpeechRecognition??w.webkitSpeechRecognition??null}
async function safeJson(response:Response){const text=await response.text();if(!text)return{};try{return JSON.parse(text)}catch{throw new Error("invalid_json_response")}}

const copy={
fr:{eyebrow:"Conversation Heni",title:"Parlez naturellement à Heni.",intro:"Utilisez le micro dans un navigateur compatible ou écrivez votre message. Les actions passent par les mêmes outils administratifs que l'application.",connect:"connexion",ready:"prêt",listen:"j'écoute",process:"je traite",speak:"je parle",assistant:"Assistant patient administratif",stop:"Arrêter",talk:"Parlez",micro:"Tunisien / Français",unsupported:"Micro non supporté",auto:"Réponse vocale automatique",placeholder:"Écrivez ou parlez…",send:"Envoyer",startError:"Heni n'est pas disponible pour le moment. Veuillez réessayer.",chatError:"Je n'ai pas pu traiter ce message. Veuillez réessayer.",micDenied:"L'accès au microphone a été refusé. Autorisez-le dans le navigateur ou continuez au clavier.",micUnsupported:"La saisie vocale n'est pas prise en charge ici. Continuez au clavier.",micEmpty:"Je n'ai rien entendu. Touchez le micro et réessayez."},
ar:{eyebrow:"محادثة هاني",title:"احكي مع هاني بطبيعتك.",intro:"استعمل الميكرو إذا المتصفح يدعمو، ولا اكتب رسالتك. هاني يستعمل نفس الأدوات الإدارية متاع التطبيق.",connect:"اتصال",ready:"حاضر",listen:"نسمع",process:"نعالج",speak:"نحكي",assistant:"مساعد إداري للمريض",stop:"وقف",talk:"احكي",micro:"تونسي / فرنسي",unsupported:"الميكرو غير مدعوم",auto:"اقرأ الرد بالصوت",placeholder:"اكتب أو احكي…",send:"ابعث",startError:"هاني موش متاح توّا. عاود حاول.",chatError:"ما نجمتش نعالج الرسالة. عاود حاول.",micDenied:"ما عناش إذن للميكرو. فعّلو من المتصفح ولا كمل بالكتابة.",micUnsupported:"الإدخال الصوتي موش مدعوم هنا. كمل بالكتابة.",micEmpty:"ما سمعت حتى كلام. اضغط على الميكرو وعاود حاول."},
en:{eyebrow:"Heni conversation",title:"Talk naturally with Heni.",intro:"Use the microphone in a compatible browser or type your message. Actions use the same administrative tools as the app.",connect:"connecting",ready:"ready",listen:"listening",process:"processing",speak:"speaking",assistant:"Patient administrative assistant",stop:"Stop",talk:"Speak",micro:"Arabic / French / English",unsupported:"Microphone unsupported",auto:"Speak replies automatically",placeholder:"Type or speak…",send:"Send",startError:"Heni is unavailable right now. Please try again.",chatError:"I couldn't process that message. Please try again.",micDenied:"Microphone access was denied. Allow it in the browser or continue by typing.",micUnsupported:"Voice input isn't supported here. Continue by typing.",micEmpty:"I didn't hear anything. Tap the microphone and try again."}}
const quick=["Nheb ناخذ rendez-vous fil imagerie","sbeh","1","اي نأكد","شنوة نجيب؟","وين نمشي؟","Nnajjem nzid dose?","نحب نحكي مع موظف"];

export function VoiceLabClient(){const {locale,rtl}=usePersistentLocale();const c=copy[locale];const [sessionId,setSessionId]=useState("");const [msgs,setMsgs]=useState<Msg[]>([]);const [input,setInput]=useState("");const [busy,setBusy]=useState(false);const [listening,setListening]=useState(false);const [speaking,setSpeaking]=useState(false);const [started,setStarted]=useState(false);const [micSupported,setMicSupported]=useState(true);const [autoSpeak,setAutoSpeak]=useState(true);const bottom=useRef<HTMLDivElement>(null);const mounted=useRef(false);const recognitionRef=useRef<any>(null);const busyRef=useRef(false);

const start=useCallback(async(preserveMessages=false)=>{setBusy(true);busyRef.current=true;try{const response=await fetch("/api/v1/voice/session",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({locale,patientId:"patient-hedi",source:"voice_lab"})});const payload=await safeJson(response);if(!response.ok||!payload.session?.id)throw new Error("session_start_failed");if(!mounted.current)return;setSessionId(payload.session.id);const greeting={role:"bot" as const,text:String(payload.message??c.startError),at:new Date().toISOString()};setMsgs(current=>preserveMessages?[...current,greeting]:[greeting]);setStarted(true);return payload.session.id as string}catch{if(mounted.current){setSessionId("");setStarted(false);const failure={role:"bot" as const,text:c.startError};setMsgs(current=>preserveMessages?[...current,failure]:[failure])}return undefined}finally{busyRef.current=false;if(mounted.current)setBusy(false)}},[locale,c.startError]);
useEffect(()=>{mounted.current=true;setMicSupported(Boolean(recognitionConstructor()));void start();return()=>{mounted.current=false;recognitionRef.current?.abort?.();recognitionRef.current?.stop?.();if(typeof window!=="undefined"&&"speechSynthesis" in window)window.speechSynthesis.cancel()}},[start]);
useEffect(()=>{bottom.current?.scrollIntoView({behavior:"smooth",block:"nearest"})},[msgs,busy,listening]);
function speak(text:string){if(typeof window==="undefined"||!("speechSynthesis" in window))return;window.speechSynthesis.cancel();const utterance=new SpeechSynthesisUtterance(text);utterance.lang=locale==="ar"?"ar-TN":locale==="fr"?"fr-FR":"en-GB";utterance.rate=.92;const voices=window.speechSynthesis.getVoices();const preferred=voices.find(v=>v.lang.toLowerCase().startsWith(utterance.lang.slice(0,2).toLowerCase()));if(preferred)utterance.voice=preferred;utterance.onstart=()=>mounted.current&&setSpeaking(true);utterance.onend=()=>mounted.current&&setSpeaking(false);utterance.onerror=()=>mounted.current&&setSpeaking(false);window.speechSynthesis.speak(utterance)}
const send=useCallback(async(provided?:string)=>{const text=(provided??input).trim();if(!text||busyRef.current)return;setMsgs(current=>[...current,{role:"user",text,at:new Date().toISOString()}]);setInput("");setBusy(true);busyRef.current=true;try{let activeId:string|undefined=sessionId||undefined;if(!activeId){busyRef.current=false;activeId=await start(true);busyRef.current=true;if(!activeId)throw new Error("session_start_failed")}let response=await fetch(`/api/v1/voice/session/${activeId}/turn`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({text})});let payload=await safeJson(response);if(response.status===404){setSessionId("");busyRef.current=false;const freshId=await start(true);busyRef.current=true;if(!freshId)throw new Error("session_restart_failed");response=await fetch(`/api/v1/voice/session/${freshId}/turn`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({text})});payload=await safeJson(response)}if(!response.ok)throw new Error("conversation_failed");const message=String(payload.message??c.chatError);if(!mounted.current)return;setMsgs(current=>[...current,{role:"bot",text:message,tool:payload.tool,at:new Date().toISOString()}]);if(autoSpeak)speak(message)}catch{if(mounted.current)setMsgs(current=>[...current,{role:"bot",text:c.chatError}])}finally{busyRef.current=false;if(mounted.current)setBusy(false)}},[autoSpeak,input,sessionId,start,c.chatError,locale]);
async function listen(){if(listening){recognitionRef.current?.stop?.();return}const Recognition=recognitionConstructor();if(!Recognition){setMicSupported(false);setMsgs(current=>[...current,{role:"bot",text:c.micUnsupported}]);return}try{if(navigator.mediaDevices?.getUserMedia){const stream=await navigator.mediaDevices.getUserMedia({audio:true});stream.getTracks().forEach(track=>track.stop())}const recognition=new Recognition();recognitionRef.current=recognition;recognition.lang=locale==="ar"?"ar-TN":locale==="fr"?"fr-FR":"en-GB";recognition.interimResults=true;recognition.continuous=false;recognition.maxAlternatives=1;recognition.onstart=()=>mounted.current&&setListening(true);recognition.onend=()=>{if(mounted.current)setListening(false);recognitionRef.current=null};recognition.onerror=(event:any)=>{if(!mounted.current)return;setListening(false);recognitionRef.current=null;const denied=event?.error==="not-allowed"||event?.error==="service-not-allowed";setMsgs(current=>[...current,{role:"bot",text:denied?c.micDenied:c.micEmpty}])};recognition.onresult=(event:any)=>{const result=event.results?.[event.results.length-1];const transcript=String(result?.[0]?.transcript??"").trim();if(!transcript)return;setInput(transcript);if(result.isFinal)void send(transcript)};recognition.start()}catch{setListening(false);setMsgs(current=>[...current,{role:"bot",text:c.micDenied}])}}
async function restart(){if(typeof window!=="undefined"&&"speechSynthesis" in window)window.speechSynthesis.cancel();recognitionRef.current?.abort?.();setSpeaking(false);setListening(false);setSessionId("");setMsgs([]);setStarted(false);await start()}
return <div className={`voice-lab-layout ${rtl?"rtl":""}`} dir={rtl?"rtl":"ltr"}><section className="voice-stage"><div className="voice-stage-top"><div><div className="eyebrow">{c.eyebrow}</div><h2>{c.title}</h2><p>{c.intro}</p></div><div className="voice-state-badge"><span className={listening?"live":busy?"busy":"ready"}/>{listening?c.listen:busy?c.process:speaking?c.speak:started?c.ready:c.connect}</div></div><div className="voice-character-zone"><div className={`voice-rings ${listening?"listen":speaking?"speak":""}`}><HeniAvatar size={156} speaking={speaking} listening={listening}/></div><div className="voice-character-copy"><strong>Heni · هاني</strong><span>{c.assistant}</span></div><div className="waveform" aria-hidden="true">{Array.from({length:22}).map((_,i)=><i key={i} style={{animationDelay:`${i*35}ms`}}/>)}</div></div><div className="voice-conversation" aria-live="polite">{msgs.map((message,index)=><div className={`voice-turn ${message.role}`} key={`${message.role}-${index}`}><div className="voice-turn-label">{message.role==="bot"?"Heni":locale==="ar"?"إنت":locale==="en"?"You":"Vous"}</div><div className="voice-turn-text">{message.text}</div>{message.tool&&<div className="voice-tool-event">✓ {message.tool}</div>}</div>)}{busy&&<div className="voice-turn bot"><div className="voice-turn-label">Heni</div><div className="typing"><span/><span/><span/></div></div>}{listening&&<div className="heni-recording-state"><span/> {c.listen}</div>}<div ref={bottom}/></div><div className="voice-controls"><button className={`talk-button ${listening?"active":""}`} onClick={()=>void listen()} disabled={busy}><span>{listening?"■":"🎙"}</span><strong>{listening?c.stop:c.talk}</strong><small>{micSupported?c.micro:c.unsupported}</small></button><label className="switch-line"><input type="checkbox" checked={autoSpeak} onChange={event=>setAutoSpeak(event.target.checked)}/><span>{c.auto}</span></label></div><div className="chat-input voice-text-input"><input className="input" value={input} onChange={event=>setInput(event.target.value)} onKeyDown={event=>{if(event.key==="Enter"){event.preventDefault();void send()}}} placeholder={c.placeholder}/><button className="btn btn-primary" onClick={()=>void send()} disabled={busy||!input.trim()}>{c.send}</button></div></section><aside className="voice-demo-rail"><div className="card voice-proof-card"><div className="eyebrow">Demo</div><h3>Access → Guidance → Continuity</h3><p className="muted tiny">The conversation uses deterministic administrative tools. Clinical decisions remain with healthcare professionals.</p></div><div className="card card-flat"><div className="row"><strong>Scénario guidé</strong><span className="badge info">8</span></div><div className="voice-shortcuts">{quick.map((phrase,index)=><button key={phrase} onClick={()=>setInput(phrase)}><span>{index+1}</span><b>{phrase}</b></button>)}</div></div><div className="stack"><button className="btn btn-secondary btn-wide" onClick={()=>void restart()}>↻ {locale==="ar"?"محادثة جديدة":locale==="en"?"New conversation":"Nouvelle conversation"}</button><a className="btn btn-primary btn-wide" href="/staff/calls">{locale==="ar"?"شوف سجل الأدوات":locale==="en"?"View tool trace":"Voir la trace outils"} →</a></div></aside></div>}