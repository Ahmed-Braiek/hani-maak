"use client";import {useState} from "react";import {useRouter} from "next/navigation";import {usePersistentLocale} from "@/lib/locale-client";import {stx} from "@/lib/staff-i18n";export function AppointmentActionClient({id,state}:{id:string;state:string}){const [busy,setBusy]=useState(false);const router=useRouter();const {locale}=usePersistentLocale();async function complete(){setBusy(true);await fetch(`/api/v1/staff/appointments/${id}/complete`,{method:"POST"});setBusy(false);router.refresh()}if(state!=="confirmed")return null;return <button className="btn btn-primary" onClick={complete} disabled={busy}>{stx(locale,busy?"staff.updating":"staff.complete")}</button>}
